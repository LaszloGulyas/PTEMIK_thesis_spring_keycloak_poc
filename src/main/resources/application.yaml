server:
  port: 8080

cors-policies: http://127.0.0.1

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/springapp
    username: springapp
    password: springapp
  jpa:
    hibernate:
      ddl-auto: create-drop

logging:
  level:
    org:
      hibernate: info
      springframework:
        security: info
        web: info
